my module MONKEY_TYPING;
# exporting constants doesn't work, having the compiler look at constants
# doesn't work, and anyway True isn't available early enough, so this whole
# thing is just fudged in the parser.

# constant $?MONKEY_TYPING is export = True;
